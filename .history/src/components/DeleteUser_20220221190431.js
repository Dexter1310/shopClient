import React, { Component } from 'react';
import { useParams } from 'react-router-dom';
import { Users } from '../Users';

const DeleteUser = () => {
    let { id } = useParams();

    return (
        <const [state, dispatch] = useReducer(first, second, third)  idDelete={id}/>
    );
}

export default DeleteUser
